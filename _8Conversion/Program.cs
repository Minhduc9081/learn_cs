
// See https://aka.ms/new-console-template for more information

namespace _8Conversion
{
    internal class Program
    {
        public static void Main(string[] args)
        {
            //Bài tập 1: Kiểm tra implicit conversion
            /*
             * int intValue = 4;
               
               long longValue = intValue;
               float floatValue = intValue;
               double doubleValue = intValue;
               
               Console.WriteLine(longValue.GetType());
               Console.WriteLine(floatValue.GetType());
               Console.WriteLine(doubleValue.GetType());
             */
            
            //Bài tập 2: Tính toán với các phép toán số học
            /*
             * Viết chương trình nhập vào hai số nguyên và thực hiện tất cả các phép toán số học: cộng, trừ, nhân, chia, và chia lấy dư.
             * In kết quả cho từng phép toán.
             *
             * Console.Write("Enter a: ");
               var a = Console.ReadLine();
               bool parsed = int.TryParse(a, out var result);
               
               Console.Write("Enter b: ");
               var b = Console.ReadLine();
               bool parsed2 = int.TryParse(b, out var result2);
               if(parsed && parsed2)
               {
                   Console.WriteLine($"{result} + {result2} = {result + result2}");
                   Console.WriteLine($"{result} - {result2} = {result - result2}");
                   Console.WriteLine($"{result} * {result2} = {result * result2}");
                   if (result2 != 0) 
                   {
                       Console.WriteLine($"{result} / {result2} = {result / result2}");
                   }
                   else
                   {
                       Console.WriteLine("Invalid input for divide. Please enter result2 > 0");
                   }
               }else
               {
                   Console.WriteLine("Invalid input. Please enter valid numbers.");
               }
             */
       
            
            
        }   
    }
}   